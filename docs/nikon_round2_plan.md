# Nikon 第二轮：crop、单光源覆盖与光源增强

Updated: 2026-09-25 · Agent: codex · Status: running

第一轮已完成，M 是合成模型中最好的组但仍未达到 baseline。以下方案已获执行授权并开始运行。启动复核为PASS / ADEQUATE、成熟度S1；参照M的开发验证已完成，C组训练已启动。这些是启动与执行证据，不是性能验收通过；尚无第二轮完整结果。本轮不改变单光源图像来源边界、固定模型、官方划分或69,600更新预算。

本次运行器SHA256：`cf33578d9e8f54b390174fefae065cbf346e075ce626ec5f55fbd255245c55d8`。执行状态是2026-09-25更新时的快照，不是实时监控。

启动前6项测试全部通过；AC完成167次真实optimizer更新校准，L/S各完成1次。校准中梯度均有限且参数变化非零。这些检查确认数值和训练路径可运行，尚不证明增强有效。

## 第一批：三个新增训练

| 组 | 相对M的变化 | 固定配置 |
|---|---|---|
| C | crop | 50%保留全图；50%在原生单光源图上裁边长为短边75–100%的正方形，再缩至256。先处理WB源图，再生成光照和标签。 |
| A | 增加单光源样本 | 25%原始单光源RAW/Light1；25%从train池取单个端点做全局relit；50%M混光。 |
| AC | 两项组合 | 同C的crop与同A的样本比例。 |

原图分支可由 `WB × Light1` 重建。A把原图和纯端点合成作为一个样本组成因素，若有收益还需拆分实验才能归因。Crop增加内容与尺度覆盖，不创造独立场景。

## 第二批：两个独立新增训练

用下述开发验证分数从M/C/A/AC选一个共同参照，然后分别加L和S，不在一个试验中同时加入两项。

- **L，端点小扰动：** 在log(R/G)、log(B/G)加sigma=0.02、截断于±2sigma的零均值高斯噪声。超出train有效端点二维凸包时最多重抽8次，仍失败则保留原端点并记录。原图分支不扰动，合成图和GT同步更新。Sigma是探索参数，不是原论文参数。
- **S，空间贡献与明暗：** 仅替换混光分支。两个4×4标准正态网格上采样为z1/z2，b~U[-2,2]；`u1=exp(b/2+0.7*z1)`，`u2=exp(-b/2+0.7*z2)`，`alpha=u1/(u1+u2)`。明暗场为`(u1+u2)/空间中位数`并限制到[0.5,2]。输入用`WB×S×E`，标签用G=1色度场E；S不进入色度GT。float32计算、整图标量归一化控制数值范围，不裁剪RGB通道。

Depth留作后续空间场的结构引导候选。本批不加入gamma或任意RGB jitter，以维持线性重光照与标签关系。全局曝光倍率大体被当前z-score消去，故不单列为增强实验。

## 固定开发验证与执行闸门

正式训练前固化验证图像/标签及哈希，并完成crop坐标一致性、原图重建、GT同步变换、端点正值、非有限统计、单步梯度和吞吐检查；保存初始权重哈希。必要参数修订先登记，再训练。

开发验证只使用官方val的真实单光源图与Light1，以及从这些源图构造的冻结合成验证集；端点仍只来自train。每val场景保持全图，生成四类各一张：全局单端点、随机方向宽sigmoid、随机中心Gaussian局部光、4×4低频场。独立固定seed=10001。

排序分数为：

`0.5 × (真实单光源val mean / M对应mean) + 0.5 × mean(四类合成val mean / M各自对应mean)`

分数越低越好；精确相同按M、C、A、AC的顺序优先保留少改动方案。L/S采用相同分数比较。这是代理验证，不能保证真实多光源泛化；真实多光源稠密GT不用于这一步选型。

候选与参数冻结后统一评分baseline、M和本轮候选，报告全部已测组及patch/pixel指标。不同方法val和test一致，val与test互斥。目标仍为接近baseline约1.969°，2.0692°只是探索标尺。只有明显改善且接近目标后才追加seed1/2，不用单seed最小值宣称优越性。

五个新增训练的原估计约一小时纯训练；crop/S吞吐须实测。当前文档不将预计耗时记作运行证据，也不把获授权等同于训练完成。
