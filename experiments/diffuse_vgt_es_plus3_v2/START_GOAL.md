# Goal 启动入口：七组匹配实验队列

日期：2026-09-26。作者：ChatGPT。研究方案，不是实验结果。

## 当前交付状态（先读）

本目录已同步的是 `GOAL_TASK.md`、`queue_plan.json` 和本启动说明。它们是完整实验定义与自动执行要求，不是已经接入本地One-Net的训练程序。

聊天端曾创建NumPy算式参考并实际通过12项合成数值单测，但参考Python写入Git的工具调用被拦截，**不宣称该脚本或测试脚本已在本目录中**。因此，任务书中提到的 `render_reference.py` 仅是可选算式参考名称，不是当前仓库依赖；本地执行应依据 `GOAL_TASK.md` 第4节的七组公式实施，并独立完成第5、6、8节的真实管线检查。不得把聊天端算式单测当作Nikon输入校准或生产运行证据。

本次会话未触发GPU、GitHub Actions或本地Codex进程。需要由本地Goal接收此任务并开始实施。用户已要求本批自动运行，所以进入本地Goal之后，在约定的范围内不逐组重复询问。

## 可直接交给Goal的指令

读取本目录 `GOAL_TASK.md` 和 `queue_plan.json`。用户要求在上一版再增加三个实验并自动运行；新组为 KEEP_OLD_R、SHAM_OLD_0、NO_NEW_S，连同原计划 ES10/ES01 和同批 B/C 参照 ES00/ES11，共七组，每组69,600成功更新。

先盘点本地已完成任务与私有生产环境，避免重复。按任务书公式将七组接入原训练器，保持原图分支、采样、标签、缓存、模型与loss，共享七组曝光与技术mask。不要等待并不存在于本目录的参考脚本，也不要跳过必要的实现与真实full/tail校准。

实现和检查通过后，在已授权资源上按 ES00、ES11、ES10、ES01、KEEP_OLD_R、SHAM_OLD_0、NO_NEW_S 顺序自动训练。不必逐组再次询问，严格遵守7组/487,200成功更新上限。性能差仍完成剩余预声明组；标签、数据、数值或预算错误则留证停止。瞬时中断只允许在冻结身份及全部RNG/优化器状态完整时每组一次精确恢复，不额外重头再训。

全部训练完成后，先冻结原开发评分和唯一候选，再统一真实val/test评价。输出所有组、四格条件效应和交互、三个新问题的配对差值，不只报最低分。不得看test后加参数、换winner、追加seed或更改验收。保留24图已知真值先验校准，仅作诊断，不用其结果修改本批参数。

在新分支 `codex/diffuse-vgt-es-plus3` 安全同步代码、冻结配置、轻量结果和结论。不要覆盖旧A/B/C与lowfreq目录，不上传RAW、权重、缓存、私有路径或凭据。若资源、权限或关键资产缺失，写BLOCKED和准确缺项；不能声称已经开跑。完成该有限队列即结束。
