# Initial independent contract and pipeline review

updated: 2026-09-26; agent: codex

This is the controller's retained summary of two separate fresh-context, read-only reviewers, not an additional independent verdict. Same model/shared filesystem: isolation limited.

`diffuse_contract_review`: **PROVISIONAL**. A/B/C render-neutrality equations are self-consistent, but common lamp power after median normalization leaves H/I/J/E unchanged; the original test wording claiming I changes is incorrect. Original DG R1 photometry exception applies only to the old two runs. Undefined prior fill/rotated validity and storage exposure positions require freezing. Only stage-zero audit and pure mathematics may proceed before these gates.

`diffuse_pipeline_audit`: parent O uses unmasked E means over entire patches (`round3_synthesis.py:131`) and patch-level nonblack/GT-component validity. Masked per-pixel means are a changed label definition, despite the task describing them as inherited. O's actual source loader is `round2.py:49–62`; its normalization pools C,H,W (`frozen/domislovic_v2.py:303–306`), not channel-wise. Existing geometry RNG also draws source order; adding draws would disturb matching. No actual mixed inputs/GT were read by either reviewer.

The contract review's preliminary statement that masked-region averaging conforms to the old interface was superseded by the later source-code audit; it must not be treated as a resolved compatibility check.

Task SHA256: `7488b5be071894e25144554ffee225188872e5656270b42eba501f699942424f`.
Source manifest SHA256: `107377500af89fc67a95a9f8095a92826957057880e2f0371baff81980c96924`.
No physical-linearity, prior-quality or downstream-performance validation granted.
