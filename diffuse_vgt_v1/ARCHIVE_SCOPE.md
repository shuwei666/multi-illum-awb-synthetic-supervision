# Archive scope

This is a local, unpublished code and lightweight evidence snapshot of the completed diffuse Virtual GT batch. Public export is held; see DELIVERY.md. It is not a standalone runnable reproduction package or a declaration that export/privacy checks passed.

The authoritative local experiment directory additionally retains source manifests, all allowed data references, prior models/caches, full optimizer/checkpoint histories, patch NPZs, calibration artifacts, and browser screenshots. RAW, model weights, caches and checkpoints are deliberately absent here. Some reproduction commands and evidence links require that full private runtime. Historical/main branches were not modified.

Numerical audit PASS/ADEQUATE is distinct from performance FINAL_TARGET_NOT_MET. Public-release checks must be revisited before any push.
