# Full-cache and actual preflight calibration-entry decision

updated: 2026-09-26; agent: codex (independent reviewer)

**PASS + ADEQUATE, S1, calibration only.** Approve A/B/C each for one cycle-0 batch-0 update and one cycle-416 batch-127 update, with separately re-created frozen initial weights and optimizer for both cases. This does not approve formal training, claim physical validation, or demonstrate downstream performance.

The repaired code decision remains in repaired_entry_review.md and is not rewritten. I independently ran remote `CUDA_VISIBLE_DEVICES='' PYTHONPATH=.:pydeps:../pydeps:../code /root/miniconda3/bin/python -c 'import train_arms as r; m=r.check_cache(); print(m["status"],len(m["files"]))'`: complete, 668; all cache-file hashes and input/provenance checks passed. No GPU work was initiated by this review. Local metadata/progress/source/cache rows agree for all668, and all diffuse authority assets match the frozen manifest. An initial local path-check attempt used an unresolved relative parent and failed; rerun with resolved root in audit_calibration_entry.py passed, without changing evidence.

Cache: 3,011,816,286 bytes, 824.24 seconds, technical unusable fraction 0. Fixed QC-grid minimum usable count is244; that is not the online patch consumption count. Freeze SHA256 is `4886059347f1ddf4f9d9f285cc21cb8a2be9a47d948ec940d98a71e1365b7803`. Cache-manifest SHA256 is `807bf9573ecd5cef4d01e5e96502461797f242dcd20023a8b54af4106cc00ae0`.

I inspected and independently asserted the completed preflight_001.json bindings and full/tail equality checks using reviews/audit_calibration_entry.py. Runtime106.32 seconds; allocated peak19,186,227,200 bytes, reserved peak19,839,057,920 bytes. Full prefix consumes1,336 views /84,983 valid patches; tail prefix1,024 views /65,136 valid patches. A/B/C common masks, endpoint ledgers, crop/lamps and sampling hashes agree; original branch and parent draws agree; A/B GT agrees. Technical rejection is zero. Both cases show nonzero finite A-B/A-C/B-C image and patch normalization differences. This is evidence that the intended intervention survives the input pipeline, not that it is beneficial. Actual optimizer memory/gradients remain the purpose of calibration.

The unchanged 24-image review is reused, not falsely described as a second independent visual examination. Its texture leakage, dark-preview limitations, geometry uncertainty, non-Lambertian regions and camera-proxy caveats remain. Full cache and finite identities do not clear these physical uncertainties. Original global-light input_signal is implementation evidence, code-inspected but not GPU-rerun by me; the newly checked actual full/tail preflight corroborates normalization sensitivity. Actual physical relighting validation remains **NOT_PERFORMED**.

Decision JSONs: reviews/A_calibration_review.json, B_calibration_review.json and C_calibration_review.json. Freeze-bound evidence wrappers: reviews/real24_freeze_bound.json and reviews/input_signal_freeze_bound.json. Runner-schema gates: frozen/A_calibration_approval.json, B_calibration_approval.json and C_calibration_approval.json. They bind hashes of preflight and wrappers; no old evidence or code was modified. The serialization/audit script lives only in reviews and cannot train or infer. Execute runner check_gate after synchronization before the actual calibration.

Correctness: repaired implementation plus actual matched preflight checked. Evidence: complete hashed cache, retained QC caveats, observed runtime signals. Reproducibility: frozen artifacts and hash-bound gates. Task fit: exactly six fresh single updates across three arms, no extra formal run or variant. Isolation is limited: fresh reviewer context/processes, shared filesystem and model.
